var numbers = [1, 2, 3, 4, 5];
// Intentionally accessing an invalid index
console.log(numbers[10]); // This will cause an array index error
// Correcting the error
console.log(numbers[2]); // Accessing a valid index

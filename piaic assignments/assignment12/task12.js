var names = ["Ahmed", "Areeb", "Hassan", "Rana"]; // Array of names
for (var i = 0; i < names.length; i++) {
    var message = "Hello, ".concat(names[i], "! Have a great vacations!"); // Personalized message
    console.log(message); // Print the personalized message
}
